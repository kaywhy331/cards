{
  "name": "ezcards",
  "version": "0.5.0",
  "private": true,
  "type": "module",
  "description": "Invite-only TCG signal intelligence, device-bound queue handoff, and an isolated AIO education lab.",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "validate": "node scripts/validate.mjs",
    "test": "node --test tests/*.test.mjs",
    "ci": "npm run validate && npm test && npm run build",
    "completion": "npm run ci"
  },
  "dependencies": {
    "@netlify/blobs": "latest"
  }
}
